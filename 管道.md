```c
int p[2];
char *argv[2];
argv[0] = "wc";
argv[1] = 0;
pipe(p); // 创建管道
if (fork() == 0) {
    close(0);
    dup(p[0]);
    close(p[0]); // (1)
    close(p[1]); // (1)
    exec("/bin/wc", argv);
} else {
    close(p[0]);
    write(p[1], "hello world\n", 12);
    close(p[1]);
}
```
### 重定向后的效果
+ stdin = p[0]
+ stdout = p[1]
### fork() 前创建的管道都是每个进程独立的
### 子进程 (1) 处之后为何还能用管道
+ `exec("/bin/wc", argv);` 替换了子进程的代码，但 **文件描述符不变**
+ `close(p[0])` 和 `close(p[0])` 可以看作减少管道的**引用计数**，不是真正关闭管道
### 使用 dup2()
```c
dup2(p[0], 0);
dup2(p[1], 1);
```
### 在不再使用管道某端时记得将他关闭
+ 保证文件标识符不会消耗的太快